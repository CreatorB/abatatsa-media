<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.AbatatsaMedia" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="AppTheme" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Customize your theme here -->
        <item name="android:colorPrimary">@color/primary</item> <!-- Your primary color -->
        <item name="android:colorPrimaryDark">@color/primary</item> <!-- Your custom status bar color -->
        <item name="android:colorAccent">@color/primary</item> <!-- Your accent color -->
        <item name="android:statusBarColor">@color/primary</item>
        <!-- Add more customizations if needed -->
    </style>
</resources>